import { Button } from '@mantine/core'
import { IconSearch } from '@tabler/icons-react'

export default function Home() {
  return (
    <Button leftSection={<IconSearch size={18} />} size="sm">
      HOHOHO
    </Button>
  )
}
